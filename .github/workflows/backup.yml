name: Scheduled Postgres Backup

on:
  schedule:
    - cron: "0 3 * * *" # daily at 03:00 UTC
  workflow_dispatch:

jobs:
  backup:
    uses: ./.github/workflows/backup-postgres.yml
    with:
      backup_dir: scheduled
      retain: 7
    secrets: inherit
